# Nixpacks configuration for Railway deployment
# This is for a monorepo - Railway sets root to frontend/

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
